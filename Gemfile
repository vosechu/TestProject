# frozen_string_literal: true

source 'https://rubygems.org'

group :development, :test do
  gem 'debug', require: false

  gem 'guard'
  gem 'notiffany' # notifications for guard
  gem 'terminal-notifier-guard'

  gem 'guard-rspec'

  gem 'rspec'

  gem 'rubocop'
  gem 'rubocop-rspec'
end
